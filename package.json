{
  "devDependencies": {
    "parcel": "1.12.3",
    "sass": "^1.32.8",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "start": "npm run clean && parcel src/index.html --open",
    "build": "npm run clean && parcel build src/index.html",
    "clean": "rm -rf dist"
  }
}
